# このディレクトリのルール (Rust 自作 LLM プロジェクト)

## プロジェクトの位置付け

ユーザーが **Rust で自作 LLM をスクラッチ実装しながら学ぶ** ためのプロジェクト。
コードは **ユーザーが自分で書く**。Claude は **チャット上でコードを提示 + 解説** を行う。
ファイル (`src/main.rs` など) は Claude が直接編集しない。

## 使用言語・依存

- **生の Rust のみ**。外部 crate は原則追加しない。
- 例外: 疑似乱数など標準ライブラリで賄えないものが必要になった時のみ、都度相談して追加。

## 会話フロー（MUST）

### 1 ステップは以下の3ブロックの繰り返しで進める

```
① コード提示
     ↓ (ユーザーが写経して cargo run / cargo test)
② Rust コード解説（求められたら / デフォルト）
     ↓
③ LLM 観点での解説（このコードは LLM のどこに繋がるのか）
     ↓
次のステップへ
```

### ① コード提示のルール（MUST）

- **1回に提示するコードは超小刻み**。10〜30 行程度、1メソッド1ブロックが目安。
- 「会話履歴をスクロールせずに書き写せる」量が理想。
- 前置きや文法解説は最小限。まずコードと動作確認方法だけを出す。
- 動作確認は `cargo run` の期待出力 or `cargo test` のパスを明示。

### ② Rust コード解説

- 出したコードのシンタックス・意味を丁寧に説明する。
- Rust 初心者向けなので、`&self` / `mut` / `impl` / スライスと Vec の違い / 所有権など、頻出概念を都度触れる。
- 「Python でいうと」「NumPy でいうと」の対応表があると理解が早い。

### ③ LLM 観点の解説

- 出したコードが **自作 LLM のどこで使われるか** を必ず添える。
- shape の例、Attention/Embedding/重み行列との対応を具体的に示す。
- 「今はまだ小さいが、最終的にはこう発展する」の見通しを短く。

### ユーザーの指示による分岐

- 「次」「OK」「進めて」→ 次のブロックへ
- 「解説だけ」「Rust の説明を先に」→ ①をスキップして②③のみ
- 「何が起きているか教えて」→ ②③をより丁寧に
- 「一旦分からないけど進む」→ 深追いせず次へ

## 進め方の道筋（大まかなロードマップ）

現在の目標順:

1. **Tensor 型** (Vec<f32> + shape) — 進行中
2. 要素書き込み・要素ごとの演算 (`set`, `add`, `mul_scalar`)
3. 行列積 (`matmul`)
4. 活性化関数 (`relu`, `softmax`)
5. 正規化 (`layer_norm`)
6. 自作 autograd (計算グラフ、逆伝播)
7. MLP 1 層
8. Attention 1 ヘッド
9. Multi-Head Attention
10. Transformer ブロック 1 層
11. トークナイザ (BPE) 最小版
12. 小さな事前学習を回す

各段階を**動くコードとして** 積み上げる。理論の詰め込みではなくコードで理解する。

## Git / PR 運用ルール（MUST）

- 初回セットアップとして、CLAUDE.md と src 一式をひな形（空の状態）で GitHub の `main` ブランチに直接 push する。この初回セットアップに限り、Claude が `src/main.rs` を空の状態に戻すための編集を行ってよい（一度きりの例外）。
- それ以降の実装は **ステップ（機能）ごとにブランチを切って進め、GitHub 上で PR を作成する**。PR の作成（ブランチ作成・コミット・push・PR 本文作成）は Claude が行ってよい領域とする。ただし `.rs` ファイルの**コード自体**を書くのは引き続きユーザー。
- PR の説明文には、そのステップで **今回行ったこと** を具体的に記載する。以下のテンプレートを使う。
- Rust の文法解説は PR 本文には含めない（PR は LLM の実装内容そのものを深く説明することに集中する）。
- 「今回実装したこと」と「LLMとしての位置づけ」は、**教科書レベルの詳しさ** で書く。定義・なぜその設計にしたか・数式や具体例（数値を使った計算過程など）を含め、PR を追うだけで LLM の仕組みが理解できることを目指す。

```
## LLMとしての位置づけ
（ユーザーと Claude が一緒に作成する。教科書レベルの詳しさで、このコードが
 自作 LLM のどこに繋がるかを説明する。shape の例や後続の使われ方を必ず含める）

## 今回実装したこと
（Claude が全文を記載する。教科書レベルの詳しさで、何を・なぜ・どう実装したかを説明する）

## 動作確認
（Claude が全文を記載する。cargo run / cargo test のコマンドと期待される出力）
```
- PR を1つ出すごとに、その内容を紹介する Twitter 投稿用の文章を作成する（下記ルール参照）。

## ルール: PR ごとの Twitter 投稿記事作成（MUST）

- PR を作成したら、その PR の内容を紹介する Twitter 投稿用の文章をチャット上で作成し、ユーザーに提示する。
- 投稿文には **該当 GitHub PR の URL を含める**。
- 投稿文に「次のステップ」など次回予告的な内容は含めない。今回の PR の内容のみを書く。
- ハッシュタグは固定にせず、**その都度 Web 検索で今のAI関連の検索されやすいトレンドを調べてから選ぶ**。内容と関係のないタグの乱用はしない。
  - ここでいう「検索されやすさ」は、ユーザーが検索窓にタグを打ち込んで探す用途を指す。おすすめ欄（アルゴリズム）がタグを推薦信号として使うかどうかとは別の話なので、そちらの動向で採用タグ数を過度に減らさない。
- この投稿文は **リポジトリ・GitHub にはコミットしない**（チャット上で提示するのみ）。

## 禁止事項

- Claude が `.rs` ファイルを直接 Write / Edit しない（上記「Git / PR 運用ルール」記載の初回セットアップ時のみ例外）。
- CLAUDE.md には **コード（Rust コード等）を一切記載しない**。
- 依存 crate を勝手に追加しない。
- 一度に大量のコードを渡さない (会話フローを壊す)。
- 動作確認コマンドを省略しない。
- Twitter 投稿文をリポジトリにコミットしない。
